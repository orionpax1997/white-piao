import { EntityConstants } from './entity-constants';

export const CONFIG_CONSTANTS: EntityConstants = {
  TABLE_NAME: 'configs',
  IDENTIFIER: '_id',
  FIELD_NAMES: ['name', 'value'],
  FIELDS: {
    NAME: 'name',
    VALUE: 'value',
  },
};
